
// EXAMPLE CODE:
const initialState = {
  email: null,
  firstName: null,
  lastName: null,
  age: null
};

export default function user (state = initialState, action) {
  switch (action.type) {
  default:
    return state;
  }
}
