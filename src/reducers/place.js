
// EXAMPLE CODE:
const initialState = {
  zip: null,
  radius: null
};

export default function place (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
